# dependencies
node_modules

# build output
dist
coverage

# vercel cli project link
.vercel

# env
.env
.env.*
!.env.example

# secret dumps written for a hosting dashboard. They hold the same values as
# .env but not the name, so the rules above miss them and `git add -A` would
# commit every production secret in one go.
*env-production*
vercel-env*
*.env.txt

# prisma generated client
/src/generated

# local tooling scaffolding (not part of the project)
.claude
.agents
.windsurf
skills-lock.json

# editors / os
.vscode
.idea
.DS_Store
*.log
